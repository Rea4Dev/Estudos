Utiliza-se `camelCase` para nome de variáveis.

Para a declaração, podemos utilizar var ou `let` (mais recomendado).

Em JavaScript, terminamos linhas com ponto e vírgula.

```JavaScript
let num1 = 10;
```